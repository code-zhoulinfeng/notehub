# 2026-09-13

- 用户确立组件约定：components/ui/ 中 shadcn 拉取的组件保持原样、不做任何源码修改；所有主题定制（颜色）通过 app/globals.css 的 Tailwind 颜色变量覆盖实现。已写入 MEMORY.md 技术约定。
- 加入 Prettier：安装 prettier + prettier-plugin-tailwindcss（devDeps），.prettierrc（printWidth 100、tailwindStylesheet 指向 app/globals.css 做类名排序），.prettierignore 排除 docs/ 和设计稿 html；新增 npm scripts：format / format:check。用户确认 components/ui 也一并格式化（纯格式化不算修改）。全量跑过一次，均为 unchanged。
- CSS 拆分：globals.css 重构为纯入口（@import tailwindcss + styles/*），实际样式拆到 app/styles/：theme/base.theme.css（NoteHub 设计系统 @theme token）、theme/shadcn.theme.css（shadcn 变量 + :root/.dark + @layer base）、base.css（元素级/滚动条）、animations.css（keyframes）、toast.css（react-toastify 适配）、components.css（nav/卡片/侧栏）、markdown.css（.md 正文）、editor.css（编辑器/Diff）。命名规范：主题文件统一 *.theme.css。Turbopack 编译通过；editor-shell/new-note-dialog 的 asChild TS 报错为拆分前已存在，与本次无关。
- dark 变体升级为「class + 系统跟随」双通道：@custom-variant dark 用块语法 + @slot（组合条件不能用 `or`，Tailwind 4.3.3 会原样输出导致 Lightning CSS 报 dangling combinator 警告）——分支一 `&:where(.dark,.dark *)`，分支二 `@media (prefers-color-scheme: dark) { &:where(:not(.light *)) }`；shadcn.theme.css 同步增加 `@media (prefers-color-scheme: dark) { :root:not(.light) {…} }` 让 CSS 变量也跟随系统。已在构建产物中验证两分支选择器均生成。
- 首页组件化（最终形态，用户两次调整后确定）：**组件作为内部函数直接写在 app/page.tsx 单文件里**，不拆独立文件、不放 features/——文件内顺序：LandingGlow（背景光晕）→ LandingHeader（顶栏）→ EntryCard（双入口卡片，variant="accent"|"plain" 区分 Owner/Visitor 配色）→ LandingPage（默认导出，纯组合）。三个独立组件文件已删除。tsc/eslint 通过（除既有 asChild 旧报错）。
- 主题变量全部改为十六进制（用户要求）：shadcn.theme.css 的 :root/.dark/@media 三处 oklch → hex（用 OKLab→sRGB 换算脚本精确转换，灰阶与 Tailwind neutral 一致：#0a0a0a/#171717/#262626/#404040/#525252/#737373/#a1a1a1/#e5e5e5/#f5f5f5/#fafafa；destructive 光 #e7000b 暗 #ff6467；sidebar-primary #1447e6；带透明度 oklch(1 0 0/10%)→#ffffff1a、/15%→#ffffff26）；base.theme.css 阴影 rgba → 8 位 hex（#e509148c/#000000e6/#000000b3）。样式文件（markdown/editor/components/base/toast.css）中的 rgba 为普通样式非主题变量，未动。构建 CSS 编译通过，asChild TS 报错为既有问题。
- 自定义工具类（用户提议）：新建 app/styles/utilities.css（globals.css 已引入），用 Tailwind 4 `@utility` 定义氛围光晕 glow-accent / glow-violet（页面级）与 glow-accent-hover / glow-white-hover（卡片 hover），rgba 全部换算为 8 位 hex；app/page.tsx 中 4 处 inline style 的 radial-gradient 全部替换为工具类，视觉零变化（已对比构建产物）。[username]/page.tsx 的 hero 渐变是单用途 banner 背景，未抽。
- VS Code 报 "Unknown at rule @utility"（css(unknownAtRules)）：内置 CSS 校验器不认识 Tailwind 4 语法的误报，已新建 .vscode/settings.json 设置 css.lint.unknownAtRules=ignore（项目级，可提交进 git 共享）。
